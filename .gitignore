# Dependencies
node_modules/

# Environment / secrets
.env
.env.*
!.env.example
!.env.test.example

# Build artifacts
dist/
build/
.next/
*.tsbuildinfo

# Logs / OS
*.log
.DS_Store

# Personal learning notes — NOT committed (kept local for study).
LEARNING_NOTES.md

# Local-only roadmap audit — kept local, not committed.
docs/roadmap/audit-2026-08-vision-alignment.md
